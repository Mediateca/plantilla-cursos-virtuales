import { InsertaHTMLDirective } from './inserta-html.directive';

describe('InsertaHTMLDirective', () => {
  it('should create an instance', () => {
    const directive = new InsertaHTMLDirective();
    expect(directive).toBeTruthy();
  });
});
